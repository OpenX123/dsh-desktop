# 项目 TODO

## dsh 共存与运行时诊断

- [ ] 区分并展示当前运行来源：客户端内置、本机 `127.0.0.1:3080` 复用实例、用户配置的远程实例。
- [ ] 通过 `host.describe` 获取并展示当前连接实例的实际 dsh 版本；继续单独展示客户端内置 dsh 版本。
- [ ] 当前实例版本与内置版本不一致时给出非阻断提示，并说明只要 API 兼容即可继续使用。
- [ ] 当其他端口存在独立 dsh 进程、客户端准备以同一个 `DSH_HOME` 启动内置实例时，评估并提示并发运行风险。
- [ ] 增加共存回归测试：只安装 npx/npm 包、默认端口实例正在运行、其他端口实例正在运行、远程连接和版本不一致。

## 官方 dsh 运行时更新

- [ ] 在设置中提供“优先使用本地已安装 dsh”选项：检测 CLI 路径与版本，并在默认端口没有可复用实例时启动该 CLI；继续保留内置运行时作为普通用户的可靠回退。
- [ ] 明确外部运行时模式的更新边界：用户通过 npm/npx 更新官方包并重启本地 dsh 后，桌面客户端无需同步升级。
- [ ] 评估内置官方运行时的独立更新通道，使后续兼容版本可以不发布新版桌面客户端；更新包须按平台构建并经过完整依赖、校验签名、兼容性冒烟、原子切换和失败回滚。
- [ ] 为运行时更新清单记录 dsh 版本、平台/架构、最低桌面客户端版本和 Node/Electron 兼容范围；不兼容时要求升级桌面客户端，而不是强行更新运行时。
